# Frontend Environment Variables
# Leave VITE_API_URL empty for Docker/Nginx proxy deployment (relative paths)
# Set to backend URL (without /api/v1) for local development or production deployment

# Local development example:
# VITE_API_URL=http://localhost:8080

# Production deployment example:
# VITE_API_URL=https://event-ingestion-backend.onrender.com

VITE_API_URL=
VITE_WS_URL=
